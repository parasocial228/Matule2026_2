<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Состояние при нажатии/выборе (белый фон с синей границей) -->
    <item android:state_pressed="true" android:drawable="@drawable/blue_border"/>
    <item android:state_selected="true" android:drawable="@drawable/blue_border"/>

    <!-- Состояние по умолчанию (синий фон) -->
    <item android:drawable="@drawable/blue_background"/>
</selector>